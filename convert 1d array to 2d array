class Solution:
    def construct2DArray(self, original, m, n):
        # Check if possible
        if len(original) != m * n:
            return []
        
        result = []
        
        # Build rows
        for i in range(m):
            row = original[i * n:(i + 1) * n]
            result.append(row)
        
        return result
